[project]
name = "sscout"
version = "0.0.1"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
"sscout" = "sscout"

[project.scripts]
sscout = "sscout.sscout:main"
